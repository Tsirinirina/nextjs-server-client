export const api = {
  baseUrl: `${process.env.API_BASE_URL}`,
  user: {
    index: `${process.env.API_BASE_URL}/user`,
  },
  frut: {
    index: `${process.env.API_BASE_URL}/frut`,
  },
};

export default api;
